import { Store } from '../store';
/**
 * Updates the application state from hashchange-events and updates the URL
 * when the page is changed internally.
 */
export default function init(store: Store): void;
